# Полный парсинг данных TrendAgent — руководство

**Задача:** Собрать ВСЕ данные по объектам недвижимости из TrendAgent, включая детальные данные, фото, планировки, видео, документы и связанные объекты.

---

## 🎯 Что будет собрано

### Базовые данные (каталог)

При запуске без флагов `--details` собираются данные из **каталога**:
- ID, название, GUID, slug
- Цены (от/до)
- Площади (от/до)
- Локация (адрес, координаты, район, метро)
- Статус, срок сдачи
- Застройщик (для ЖК)
- Основные характеристики

### Детальные данные (--details)

При добавлении флага `--details` для каждого объекта выполняется **дополнительный запрос detail**, который возвращает:

#### Для ВСЕХ типов объектов:
- ✅ Полное описание объекта
- ✅ Все поля Entity (см. TRENDAGENT_DATA_REFERENCE.md)
- ✅ Медиа: фото, видео, документы, планировки
- ✅ Метаданные о полноте данных

#### Дополнительно для ЖК (blocks):
- ✅ **advantages** — преимущества и особенности ЖК
- ✅ **finishings** — варианты отделки
- ✅ **nearbyPlaces** — инфраструктура рядом (школы, магазины, транспорт)
- ✅ **geoBuildings** — геометрия корпусов (координаты для карты)
- ✅ **bank** — банковские продукты
- ✅ **mortgage** — ипотечные программы
- ✅ **buildings** — список корпусов ЖК
- ✅ **sections** — секции/подъезды
- ✅ **plans** — планы территории
- ✅ **progress** — фото хода строительства (по годам)

**Итого для ЖК:** агрегация данных с **22 endpoint'ов** разных доменов API.

### Медиа (--media)

При добавлении флага `--media` (если нет `--details`) дополнительно собираются:
- 📷 **Фото** — все изображения объекта
- 📐 **Планировки** — поэтажные планы
- 🎥 **Видео** — промо-ролики, обзоры
- 📄 **Документы** — PDF-файлы (декларации, презентации)
- 🏗️ **Прогресс строительства** — фото по годам (для ЖК)

---

## 🚀 Команды для запуска

### 1. Полный парсинг с ВСЕМИ данными (рекомендуется)

```bash
cd backend

# ВСЕ данные для СПб (детали + медиа)
php artisan trendagent:parse --details --media

# ВСЕ данные для нескольких регионов
php artisan trendagent:parse --region=spb --region=msk --region=krd --details --media

# Только ЖК с полными данными (22 endpoint'а)
php artisan trendagent:parse --type=blocks --details

# Только ЖК и квартиры для СПб
php artisan trendagent:parse --type=blocks --type=apartments --details --media
```

### 2. Быстрый тест (проверка работы)

```bash
# Получить по 10 объектов каждого типа с полными данными
php artisan trendagent:parse --limit=10 --details --media

# Только 5 ЖК с детализацией
php artisan trendagent:parse --type=blocks --limit=5 --details
```

### 3. Парсинг без детализации (быстрый)

```bash
# Только базовые данные из каталога (быстро)
php artisan trendagent:parse

# С медиа, но без детальных данных
php artisan trendagent:parse --media
```

---

## 📊 Время выполнения

| Режим | Объектов | Время | Пример |
|-------|----------|-------|--------|
| Каталог (быстрый) | 100 ЖК | ~10 сек | `--limit=100` |
| Каталог (быстрый) | 1000 квартир | ~1 мин | `--type=apartments --limit=1000` |
| Каталог (полный СПб) | ~60 тыс. | ~10-15 мин | без флагов |
| С деталями | 100 ЖК | ~5-7 мин | `--type=blocks --limit=100 --details` |
| С деталями | 1000 квартир | ~15-20 мин | `--type=apartments --limit=1000 --details` |
| **Полный с деталями (СПб)** | **~60 тыс.** | **~2-3 часа** | `--details --media` |

**Рекомендация:** Для полного парсинга запускайте команду ночью или в фоновом режиме.

---

## 📁 Где хранятся данные

### Путь к файлам

```
storage/app/trendagent/parser/
├── spb/
│   ├── spb_blocks.json
│   ├── spb_apartments.json
│   ├── spb_parking.json
│   ├── spb_houses.json
│   ├── spb_plots.json
│   ├── spb_commerce.json
│   ├── spb_house_projects.json
│   └── spb_villages.json
├── msk/
│   └── ...
└── krd/
    └── ...
```

### Структура файла (с --details)

```json
{
  "region": "spb",
  "city_id": "58c665588b6aa52311afa01b",
  "type": "blocks",
  "label": "Комплексы (ЖК)",
  "total_in_api": 344,
  "parsed": 344,
  "timestamp": "2026-02-09T18:30:00+00:00",
  "data": [
    {
      "id": "59fc27538bcb2468a6174402",
      "entity": {
        "id": "59fc27538bcb2468a6174402",
        "name": "Villa Marina",
        "description": "Элитный жилой комплекс...",
        "price_from": 5000000,
        "price_to": 15000000,
        "area_from": 45.5,
        "area_to": 125.8,
        "location": {
          "address": "Санкт-Петербург, Приморский район",
          "coordinates": {"lat": 59.987, "lng": 30.234},
          "metro": [
            {"name": "Комендантский проспект", "distance": 850}
          ]
        },
        "developer": "Setl Group",
        "deadline": "2025-12-31",
        "apartments_count": 450,
        "buildings_count": 3,
        "floors_count": 25,
        ...
      },
      "media": {
        "photos": [
          {
            "url": "https://api.trendagent.ru/media/blocks/59fc.../photo1.jpg",
            "type": "facade",
            "title": "Фасад здания"
          },
          ...
        ],
        "videos": [
          {
            "url": "https://video.trendagent.ru/blocks/59fc.../promo.mp4",
            "title": "Промо-видео"
          }
        ],
        "floorPlans": [...],
        "documents": [...],
        "progress": {
          "2024": [...],
          "2025": [...]
        }
      },
      "related": {
        "advantages": [
          {"title": "Собственная набережная", "description": "..."},
          {"title": "Детские площадки", "description": "..."}
        ],
        "finishings": [
          {"type": "Без отделки", "price": 0},
          {"type": "Под ключ", "price": 25000}
        ],
        "nearbyPlaces": [
          {"type": "school", "name": "МБОУ СОШ №123", "distance": 500},
          {"type": "shop", "name": "Перекресток", "distance": 200}
        ],
        "geoBuildings": [
          {
            "building_id": "1",
            "coordinates": [
              [59.987, 30.234],
              [59.988, 30.235],
              ...
            ]
          }
        ],
        "bank": {...},
        "mortgage": {...},
        "buildings": [...],
        "sections": [...]
      },
      "meta": {
        "object_type": "blocks",
        "id": "59fc27538bcb2468a6174402",
        "is_complete": true,
        "failed_endpoints": []
      }
    }
  ]
}
```

---

## 🔧 Что делать с данными

### 1. Импорт в БД

```php
$file = storage_path('app/trendagent/parser/spb/spb_blocks.json');
$data = json_decode(file_get_contents($file), true);

foreach ($data['data'] as $item) {
    // Импорт в вашу БД
    Block::updateOrCreate(
        ['external_id' => $item['id']],
        [
            'name' => $item['entity']['name'],
            'description' => $item['entity']['description'],
            'price_from' => $item['entity']['price_from'],
            // ... остальные поля
        ]
    );
}
```

### 2. API для фронтенда

```php
Route::get('/blocks', function() {
    $file = storage_path('app/trendagent/parser/spb/spb_blocks.json');
    $data = json_decode(file_get_contents($file), true);
    return response()->json($data['data']);
});
```

### 3. Поиск и фильтрация

```php
$blocks = json_decode(file_get_contents($file), true)['data'];

// Фильтр по цене
$filtered = array_filter($blocks, function($block) {
    return $block['entity']['price_from'] >= 3000000 
        && $block['entity']['price_from'] <= 10000000;
});

// Поиск по названию
$search = array_filter($blocks, function($block) {
    return stripos($block['entity']['name'], 'Marina') !== false;
});
```

---

## ⚠️ Важные замечания

### Авторизация

Перед парсингом настройте авторизацию в `.env`:

```env
TRENDAGENT_PHONE=+79045393434
TRENDAGENT_PASSWORD=your_password
```

Или используйте client_id + client_secret.

### Размер данных

- **Базовый каталог** (без --details): ~1-5 МБ на 1000 объектов
- **С детализацией** (--details): ~10-50 МБ на 1000 объектов
- **Полный СПб** с деталями: ~500 МБ - 2 ГБ

Убедитесь, что есть достаточно места на диске.

### Обработка ошибок

Если парсинг прерывается:
- Частично полученные данные сохраняются
- Ошибки логируются в `storage/logs/laravel.log`
- Можно перезапустить команду — она перезапишет файлы

---

## 📋 Чек-лист полного парсинга

- [ ] Настроена авторизация в `.env`
- [ ] Проверено соединение: `php artisan trendagent:test parking`
- [ ] Есть достаточно места на диске (~5-10 ГБ для всех регионов с деталями)
- [ ] Запущен парсинг с `--details --media`
- [ ] Дождаться завершения (2-3 часа для СПб)
- [ ] Проверить файлы в `storage/app/trendagent/parser/`
- [ ] Импортировать в БД или использовать напрямую

---

## 🎯 Итоговая команда для полного парсинга

```bash
# Запустить в фоне (Linux/Mac)
nohup php artisan trendagent:parse --details --media > parser.log 2>&1 &

# Или через screen (Linux)
screen -S parser
php artisan trendagent:parse --details --media
# Ctrl+A, D для отключения

# Windows (PowerShell)
Start-Job -ScriptBlock { 
    cd C:\OSPanel\domains\figma-trendagent\backend
    php artisan trendagent:parse --details --media 
}
```

**Результат:** Полная база данных недвижимости из TrendAgent со всеми деталями, фото, планировками и связанными объектами.

---

**См. также:**
- [PARSER_COMMAND.md](backend/PARSER_COMMAND.md) — детальная документация по команде
- [TRENDAGENT_DATA_REFERENCE.md](TRENDAGENT_DATA_REFERENCE.md) — справочник всех полей
- [TRENDAGENT_SSO_README.md](backend/TRENDAGENT_SSO_README.md) — настройка авторизации
