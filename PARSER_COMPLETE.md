# ✅ Парсер TrendAgent — готов к использованию

## 🎯 Что реализовано

Создана команда `php artisan trendagent:parse`, которая собирает **ВСЕ данные** из TrendAgent API:

### ✅ Базовый режим (без флагов)
```bash
php artisan trendagent:parse
```
Собирает данные из **каталога**: ID, название, цены, площади, локация, статус, застройщик, основные характеристики.

### ✅ Режим полных данных (--details)
```bash
php artisan trendagent:parse --details
```
Для каждого объекта выполняет **дополнительный запрос detail** и собирает:
- ✅ Все поля Entity (см. TRENDAGENT_DATA_REFERENCE.md)
- ✅ Медиа: фото, видео, документы, планировки
- ✅ Для **ЖК**: агрегация **22 endpoint'ов**:
  - advantages (преимущества)
  - finishings (отделка)
  - nearbyPlaces (инфраструктура)
  - geoBuildings (геометрия корпусов)
  - bank (банк)
  - mortgage (ипотека)
  - buildings (корпуса)
  - sections (секции)
  - plans (планы)
  - progress (фото строительства по годам)
  - и другие

### ✅ Режим медиа (--media)
```bash
php artisan trendagent:parse --media
```
Дополнительно собирает:
- 📷 Фото (все изображения)
- 📐 Планировки (поэтажные планы)
- 🎥 Видео (промо, обзоры)
- 📄 Документы (PDF)
- 🏗️ Прогресс строительства (по годам)

---

## 🚀 Примеры использования

### Быстрый тест
```bash
# 10 объектов каждого типа с полными данными
php artisan trendagent:parse --limit=10 --details --media
```

### Полный парсинг с ВСЕМИ данными
```bash
# Для СПб (рекомендуется)
php artisan trendagent:parse --details --media

# Для нескольких регионов
php artisan trendagent:parse --region=spb --region=msk --region=krd --details --media

# Только ЖК с полной агрегацией
php artisan trendagent:parse --type=blocks --details

# Только важные типы
php artisan trendagent:parse --type=blocks --type=apartments --details --media
```

### Быстрый парсинг (без детализации)
```bash
# Базовые данные (быстро)
php artisan trendagent:parse

# С лимитом
php artisan trendagent:parse --limit=1000
```

---

## 📊 Результат выполнения

После завершения команды выводится таблица:

```
+----------------------------+-------------+
| Тип объекта                | Всего в API |
+----------------------------+-------------+
| Комплексы (ЖК)             | 344         |
| Квартиры                   | 55 507      |
| Паркинги (машиноместа)     | 40          |
| Дома                       | 344         |
| Участки                    | 344         |
| Коммерция (помещения)      | 168         |
| Проекты домов              | 160         |
| Поселки                    | 89          |
+----------------------------+-------------+

ℹ️  Для каждого объекта получены детальные данные:
  - Все поля entity
  - Медиа (фото, видео, документы, планировки)
  - Связанные данные (для ЖК: advantages, finishings, geo, buildings и т.д.)
  - Агрегация всех endpoint'ов (для blocks - 22 источника)
```

---

## 📁 Где найти данные

```
storage/app/trendagent/parser/
├── spb/
│   ├── spb_blocks.json          ← ЖК с полными данными
│   ├── spb_apartments.json      ← Квартиры
│   ├── spb_parking.json         ← Паркинги
│   ├── spb_houses.json          ← Дома
│   ├── spb_plots.json           ← Участки
│   ├── spb_commerce.json        ← Коммерция
│   ├── spb_house_projects.json  ← Проекты домов
│   └── spb_villages.json        ← Поселки
├── msk/
│   └── ...
└── krd/
    └── ...
```

### Структура файла (с --details)

```json
{
  "region": "spb",
  "city_id": "58c665588b6aa52311afa01b",
  "type": "blocks",
  "total_in_api": 344,
  "parsed": 344,
  "timestamp": "2026-02-09T18:30:00+00:00",
  "data": [
    {
      "id": "59fc...",
      "entity": { /* все поля объекта */ },
      "media": { /* фото, видео, планировки */ },
      "related": { /* связанные данные */ },
      "meta": { /* метаданные */ }
    }
  ]
}
```

---

## ⏱️ Время выполнения

| Режим | Объёмобъектов | Время | Команда |
|-------|----------|-------|---------|
| Тест | 10 шт. с деталями | ~30 сек | `--limit=10 --details` |
| Быстрый | 100 ЖК | ~10 сек | `--type=blocks --limit=100` |
| С деталями | 100 ЖК | ~5-7 мин | `--type=blocks --limit=100 --details` |
| Полный СПб | ~60 тыс. | ~10-15 мин | без флагов |
| **Полный СПб + детали** | **~60 тыс.** | **~2-3 часа** | `--details --media` |

---

## 📝 Требования перед запуском

### 1. Настройте авторизацию в `.env`

```env
TRENDAGENT_PHONE=+79045393434
TRENDAGENT_PASSWORD=your_password
```

Или используйте:
```env
TRENDAGENT_CLIENT_ID=your_client_id
TRENDAGENT_CLIENT_SECRET=your_secret
```

### 2. Проверьте соединение

```bash
cd backend
php artisan trendagent:test parking
```

Должно вывести: `✅ OK`, `Total: N` (N > 0).

### 3. Запустите парсинг

```bash
# Быстрый тест
php artisan trendagent:parse --limit=10 --details --media

# Полный парсинг
php artisan trendagent:parse --details --media
```

---

## 📚 Документация

1. **FULL_PARSING_GUIDE.md** — полное руководство по парсингу
2. **backend/PARSER_COMMAND.md** — детальная документация команды
3. **TRENDAGENT_DATA_REFERENCE.md** — справочник всех полей (440 строк)
4. **backend/TRENDAGENT_SSO_README.md** — настройка авторизации

---

## ✨ Что можно делать с данными

### Импорт в БД
```php
$data = json_decode(file_get_contents(
    storage_path('app/trendagent/parser/spb/spb_blocks.json')
), true);

foreach ($data['data'] as $item) {
    Block::create([
        'external_id' => $item['id'],
        'data' => $item // или разобрать по полям
    ]);
}
```

### API для фронтенда
```php
Route::get('/api/blocks', function() {
    return json_decode(file_get_contents(
        storage_path('app/trendagent/parser/spb/spb_blocks.json')
    ), true)['data'];
});
```

### Поиск и фильтрация
```php
$blocks = collect($data['data']);

// Фильтр по цене
$filtered = $blocks->filter(fn($b) => 
    $b['entity']['price_from'] >= 3000000
);

// Поиск
$search = $blocks->filter(fn($b) => 
    str_contains($b['entity']['name'], 'Marina')
);
```

---

## 🎯 Итог

✅ Команда полностью готова  
✅ Поддерживает 8 типов объектов  
✅ Работает с 9 регионами России  
✅ Собирает ВСЕ данные (детали + медиа + связанные)  
✅ Для ЖК: агрегация 22 endpoint'ов  
✅ Прогресс-бары и статистика  
✅ Обработка ошибок  
✅ Полная документация  

**Запуск:** `php artisan trendagent:parse --details --media`

**Результат:** Полная база данных недвижимости из TrendAgent со всеми фото, планировками и свойствами.
