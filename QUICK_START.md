# 🚀 QUICK START - Vue.js Admin Panel

## Быстрый старт за 3 шаги

### 1️⃣ Установка зависимостей

```bash
cd backend
npm install
```

### 2️⃣ Запуск Development сервера

```bash
# Terminal 1: Vite dev server
npm run dev

# Terminal 2: Laravel server
php artisan serve
```

### 3️⃣ Открыть в браузере

```
http://127.0.0.1:8000/admin
```

---

## 📦 Production Build

```bash
npm run build
```

Скомпилированные файлы появятся в `public/build/`

---

## 🎯 Структура админ-панели

| Раздел | URL | Описание |
|--------|-----|----------|
| 📊 Dashboard | `/admin` | Статистика по всем объектам |
| 🏢 ЖК | `/admin/blocks` | Жилые комплексы |
| 🏠 Квартиры | `/admin/apartments` | Квартиры |
| 🚗 Паркинг | `/admin/parking` | Парковочные места |
| 🏡 Дома | `/admin/houses` | Дома |
| 🌳 Участки | `/admin/plots` | Земельные участки |
| 🏪 Коммерция | `/admin/commerce` | Коммерческая недвижимость |
| 🏘️ Поселки | `/admin/villages` | Коттеджные поселки |
| 📐 Проекты | `/admin/house-projects` | Проекты домов |

---

## 🔧 Основные файлы

```
backend/resources/js/
├── app.js              # Точка входа
├── router/index.js     # Маршруты
├── services/api.js     # API клиент
├── stores/app.js       # Глобальное состояние
└── pages/              # Все страницы
```

---

## 🎨 Tailwind CSS

Все стили автоматически компилируются через Vite + Tailwind CSS 4

---

## ⚡ Горячие клавиши

- `Ctrl + C` - остановить dev server
- `Ctrl + Shift + R` - полная перезагрузка страницы

---

## 🐛 Troubleshooting

**Проблема:** Стили не загружаются
```bash
npm run build
```

**Проблема:** Ошибка при загрузке данных
- Проверьте `.env` файл
- Убедитесь что `TRENDAGENT_CLIENT_ID` и `TRENDAGENT_CLIENT_SECRET` установлены

**Проблема:** Vite не запускается
```bash
rm -rf node_modules
npm install
```

---

✅ **ВСЁ ГОТОВО К РАБОТЕ!**
