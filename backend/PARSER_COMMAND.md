# Команда парсинга данных TrendAgent

## Описание

Команда `trendagent:parse` позволяет собрать все данные по объектам недвижимости из TrendAgent API и сохранить их в файлы для дальнейшего использования.

## Использование

### Базовый запуск (спарсить все типы для СПб)

```bash
php artisan trendagent:parse
```

### С указанием регионов

```bash
# Один регион
php artisan trendagent:parse --region=spb

# Несколько регионов
php artisan trendagent:parse --region=spb --region=msk --region=krd
```

### С указанием типов объектов

```bash
# Только ЖК и квартиры
php artisan trendagent:parse --type=blocks --type=apartments

# Только паркинги
php artisan trendagent:parse --type=parking
```

### С лимитом

```bash
# Получить максимум 1000 объектов каждого типа
php artisan trendagent:parse --limit=1000

# Получить максимум 500 объектов для СПб и Москвы
php artisan trendagent:parse --region=spb --region=msk --limit=500
```

### С настройкой размера страницы

```bash
# Получать по 50 объектов за запрос (по умолчанию 100)
php artisan trendagent:parse --per-page=50
```

### С детальными данными (рекомендуется для полного парсинга)

```bash
# Получить ВСЕ данные: детали + медиа + связанные объекты
php artisan trendagent:parse --details --media

# Только детали (без дополнительного запроса медиа)
php artisan trendagent:parse --details

# Только медиа (фото, планировки) без детальных данных
php artisan trendagent:parse --media
```

**⚠️ Важно:** Режим `--details` выполняет **дополнительный запрос detail** для каждого объекта. Это медленнее, но позволяет получить:
- Все поля объекта (не только из каталога)
- Связанные данные (для ЖК: преимущества, отделка, инфраструктура, геометрия)
- Агрегацию всех endpoint'ов (для blocks — 22 источника)
- Медиа автоматически включается

## Параметры

| Параметр | Описание | Значения | По умолчанию |
|----------|----------|----------|--------------|
| `--region` | ID региона для парсинга | spb, msk, krd, rnd, crimea, kzn, ufa, ekb, nsk | spb |
| `--type` | Тип объектов | blocks, apartments, parking, houses, plots, commerce, house_projects, villages | все типы |
| `--limit` | Максимум объектов каждого типа | число (0 = без ограничений) | 0 |
| `--per-page` | Количество объектов за один запрос к API | число | 100 |
| `--details` | Получить детальные данные для каждого объекта | — | false |
| `--media` | Получить медиа (фото, планировки, видео) | — | false |

## Типы объектов

| Код | Название |
|-----|----------|
| `blocks` | Комплексы (ЖК) |
| `apartments` | Квартиры |
| `parking` | Паркинги (машиноместа) |
| `houses` | Дома |
| `plots` | Участки |
| `commerce` | Коммерция (помещения) |
| `house_projects` | Проекты домов |
| `villages` | Поселки |

## Регионы

| Код | ID города | Название |
|-----|-----------|----------|
| `spb` | 58c665588b6aa52311afa01b | Санкт-Петербург |
| `msk` | 5a5cb42159042faa9a218d04 | Москва |
| `krd` | 604b5243f9760700074ac345 | Краснодарский край |
| `rnd` | 61926fb5bb267a0008de132b | Ростов-на-Дону |
| `crimea` | 682700dd0e7daf77097d0779 | Крым |
| `kzn` | 642157fca50429d21e3aa14f | Казань |
| `ufa` | 674eff862307c824cf56ced3 | Уфа |
| `ekb` | 650974f78d34c0f790a012a9 | Екатеринбург |
| `nsk` | 618120c1a56997000866c4d8 | Новосибирск |

## Формат выходных файлов

Файлы сохраняются в `storage/app/trendagent/parser/{region}/{region}_{type}.json`

**Пример:** `storage/app/trendagent/parser/spb/spb_blocks.json`

### Базовая структура (без --details)

```json
{
  "region": "spb",
  "city_id": "58c665588b6aa52311afa01b",
  "type": "blocks",
  "label": "Комплексы (ЖК)",
  "total_in_api": 344,
  "parsed": 344,
  "timestamp": "2026-02-09T15:30:00+00:00",
  "data": [
    {
      "id": "59fc27538bcb2468a6174402",
      "name": "Villa Marina",
      "guid": "villa-marina",
      "price_from": 5000000,
      "location": {
        "address": "...",
        "coordinates": {"lat": 59.9, "lng": 30.3}
      },
      ...
    }
  ]
}
```

### Расширенная структура (с --details)

```json
{
  "region": "spb",
  "city_id": "58c665588b6aa52311afa01b",
  "type": "blocks",
  "label": "Комплексы (ЖК)",
  "total_in_api": 344,
  "parsed": 344,
  "timestamp": "2026-02-09T15:30:00+00:00",
  "data": [
    {
      "id": "59fc27538bcb2468a6174402",
      "entity": {
        "id": "59fc27538bcb2468a6174402",
        "name": "Villa Marina",
        "guid": "villa-marina",
        "description": "Полное описание ЖК...",
        "price_from": 5000000,
        "location": {...},
        "developer": "...",
        "deadline": "2025-12-31",
        ...
      },
      "media": {
        "photos": [
          {"url": "https://...", "type": "main"},
          {"url": "https://...", "type": "plan"}
        ],
        "videos": [...],
        "documents": [...],
        "floorPlans": [...],
        "progress": {...}
      },
      "related": {
        "advantages": [...],
        "finishings": [...],
        "nearbyPlaces": [...],
        "geoBuildings": [...],
        "bank": {...},
        "mortgage": {...},
        "buildings": [...],
        "sections": [...]
      },
      "meta": {
        "object_type": "blocks",
        "id": "59fc27538bcb2468a6174402",
        "is_complete": true,
        "failed_endpoints": []
      }
    }
  ]
}
```

### Что включается в детальные данные (--details)

#### Для всех типов:
- **entity** — все поля объекта из детальной страницы
- **media** — фото, видео, документы, планировки
- **meta** — информация о полноте данных

#### Дополнительно для ЖК (blocks):
- **advantages** — преимущества ЖК
- **finishings** — варианты отделки
- **nearbyPlaces** — инфраструктура рядом
- **geoBuildings** — геометрия корпусов (координаты)
- **bank** — банковские продукты
- **mortgage** — ипотечные программы
- **buildings** — корпуса ЖК
- **sections** — секции/подъезды

Всего для ЖК агрегируется **22 endpoint'а** с разных доменов TrendAgent API.

## Вывод команды

После выполнения команда выводит итоговую таблицу:

```
📊 Итоговая статистика:

+----------------------------+-------------+
| Тип объекта                | Всего в API |
+----------------------------+-------------+
| Комплексы (ЖК)             | 344         |
| Квартиры                   | 55 507      |
| Паркинги (машиноместа)     | 40          |
| Дома                       | 344         |
| Участки                    | 344         |
| Коммерция (помещения)      | 168         |
| Проекты домов              | 160         |
| Поселки                    | 89          |
+----------------------------+-------------+
```

## Примеры использования

### Полный парсинг с ВСЕМИ данными (рекомендуется)

```bash
# Получить ВСЕ: детали, медиа, связанные данные для СПб
php artisan trendagent:parse --details --media

# То же для нескольких регионов
php artisan trendagent:parse --region=spb --region=msk --details --media
```

### Полный парсинг всех регионов и типов (базовый)

```bash
php artisan trendagent:parse \
  --region=spb \
  --region=msk \
  --region=krd \
  --region=rnd \
  --region=crimea \
  --region=kzn \
  --region=ufa \
  --region=ekb \
  --region=nsk
```

### Парсинг только важных типов с детальными данными

```bash
php artisan trendagent:parse --type=blocks --type=apartments --details
```

### Быстрый тест (по 10 объектов с полными данными)

```bash
php artisan trendagent:parse --limit=10 --details --media
```

### Только ЖК с полной агрегацией (22 endpoint'а)

```bash
php artisan trendagent:parse --type=blocks --details
```

## Требования

- ✅ Настроенная авторизация TrendAgent (TRENDAGENT_PHONE + TRENDAGENT_PASSWORD или TRENDAGENT_CLIENT_ID + TRENDAGENT_CLIENT_SECRET)
- ✅ Права на запись в `storage/app/trendagent/parser/`
- ✅ Доступ к TrendAgent API

## Логирование

Все ошибки парсинга логируются в `storage/logs/laravel.log` с контекстом:

```php
[
    'region' => 'spb',
    'type' => 'blocks',
    'error' => 'Auth token expired',
]
```

## Производительность

### Без --details (только каталог):
- Между запросами к API есть задержка **0.1 секунды** (100 мс)
- При `per_page=100` для 10 000 объектов: ~100 запросов, ~10 секунд
- Полный парсинг СПб (все типы): **~10-15 минут**

### С --details (детальные данные):
- Для каждого объекта делается **дополнительный запрос detail**
- Для ЖК (blocks) агрегируется **22 endpoint'а** параллельно
- Между запросами задержка 0.1 сек
- Для 100 ЖК с деталями: **~5-7 минут**
- Для 1000 квартир с деталями: **~15-20 минут**
- Полный парсинг СПб с деталями (все типы): **~2-3 часа**

**Рекомендации:**
- Используйте `--limit` для тестирования
- Запускайте полный парсинг ночью или в фоне
- Для больших объёмов разделите на несколько команд по типам

## Обработка ошибок

Если при парсинге конкретного типа произошла ошибка:

- Команда продолжит работу с остальными типами
- Ошибка будет залогирована
- В таблице для этого типа будет 0 объектов
- Частично полученные данные будут сохранены

## См. также

- [TRENDAGENT_DATA_REFERENCE.md](../TRENDAGENT_DATA_REFERENCE.md) — полный список получаемых данных
- [TRENDAGENT_SSO_README.md](TRENDAGENT_SSO_README.md) — настройка авторизации
- [ENTITY_LAYER_REPORT.md](../ENTITY_LAYER_REPORT.md) — структура Entity
